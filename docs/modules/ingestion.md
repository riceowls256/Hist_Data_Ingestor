# Ingestion Module

Handles data fetching, normalization, and integration from external APIs.

## Key Components
- `data_fetcher.py`: Fetches and normalizes data from sources.
- `api_adapters/`: Contains adapters for specific APIs. 