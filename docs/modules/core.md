# Core Module

The core module provides foundational utilities and configuration management for the project.

## Key Components
- `config_manager.py`: Handles loading and validation of system configuration.
- `pipeline_orchestrator.py`: Orchestrates the data pipeline execution.
- `module_loader.py`: Dynamically loads pipeline modules. 